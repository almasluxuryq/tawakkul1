# Как выложить обновление на сайт (twkklbrand.com)

Бисмилля. Ниже — 3 простых шага. Шаги 1–2 делаются в **вашем PowerShell** на компьютере
(жмёте Пуск → пишете PowerShell → Enter). Шаг 3 — в веб-консоли Hetzner.

---

## Шаг 1. Скопировать новые фото худи в проект

Скопируйте весь блок ниже и вставьте в PowerShell, нажмите Enter:

```powershell
$src = "C:\Users\Almas\Downloads\hfss"
$dst = "C:\Users\Almas\Downloads\Tawakkul (2)\Tawakkul\Tawakkul\public\photos"
Copy-Item "$src\IMG_4716.JPG","$src\IMG_4717.JPG","$src\IMG_4719.JPG","$src\IMG_1827.JPG","$src\IMG_2527.JPG","$src\IMG_2515 (3).JPEG" $dst -Force
Write-Host "Фото скопированы"
```

Должно появиться «Фото скопированы». Это кладёт 6 фото в папку сайта.

---

## Шаг 2. Отправить изменения на GitHub

Скопируйте и вставьте по очереди (или все сразу):

```powershell
git -C "C:\Users\Almas\Downloads\Tawakkul (2)\Tawakkul\Tawakkul" add -A
git -C "C:\Users\Almas\Downloads\Tawakkul (2)\Tawakkul\Tawakkul" commit -m "About+FAQ, юр. страницы, Telegram-дроп, уведомление о заказе, фото худи, цена 3990"
git -C "C:\Users\Almas\Downloads\Tawakkul (2)\Tawakkul\Tawakkul" push origin main
```

Если в конце написано `main -> main` — всё ушло на GitHub. ✅

---

## Шаг 3. Собрать сайт на сервере (веб-консоль Hetzner)

Откройте консоль сервера и вставляйте команды **по одной** (по очереди, каждую с Enter).
Так надёжнее — консоль Hetzner иногда портит длинные команды.

```
cd /var/www/twkkl-new
```
```
git fetch origin
```
```
git reset --hard origin/main
```
```
pnpm build
```
```
pm2 restart twkkl-new
```

Готово. Сайт обновится. Если `pnpm build` выдаст ошибку — **старый сайт останется работать**
(перезапуск идёт только после успешной сборки), напишите мне текст ошибки.

> Если вдруг `pnpm build` жалуется на пакеты — один раз выполните `pnpm install`, потом снова `pnpm build`.

---

## Что нового в этом обновлении

- Худи ONE UMMAH — **первый** на главной, цена **3 990 ₽ / 23 990 ₸ / 54 $**, все размеры S–M–L–XL, без выбора цвета.
- Шорты AWRAH — вторые; для Чёрного и Navy размеры M и L помечены как распроданные (доступен XL).
- Новые премиальные фото худи в галерее и в секции «философия бренда».
- Новые страницы: **О бренде** (`/about`), **Вопросы/FAQ** (`/faq`) — на 3 языках.
- Юридические страницы: **Конфиденциальность** (`/privacy`), **Условия** (`/terms`), **Доставка и возврат** (`/delivery`) — в футере.
- Секция **«Дроп в Telegram»** на главной — воронка на подписку.
- На странице «заказ оформлен» кнопки WhatsApp/Telegram теперь **сами подставляют все детали заказа** (номер, состав, имя, телефон, город, сумма) — вам сразу приходит готовое сообщение.
- Убран сторонний скрипт аналитики (вы сказали, что не нужен).

---

## Безопасность (по желанию, разово)

1. **Пароль админки.** В репозитории есть `set-admin.sh`. На сервере:
   ```
   bash set-admin.sh twkkl2024almas
   ```
   (поменяет пароль входа в админку и перезапустит сайт).

2. **Бэкап базы (по желанию).** В репозитории есть `backup.sh`. На сервере:
   ```
   bash backup.sh
   ```
   Сделает копию базы в `/var/backups/twkkl` и оставит последние 14 копий.
   Можно позже поставить в автозапуск (cron) раз в день — скажите, помогу настроить.

3. **Токен Hetzner** из старой переписки уже стоит удалить/отозвать в панели Hetzner, если ещё не сделали.

---

## Про Telegram

Сейчас кнопка «Подписаться в Telegram» ведёт на **@tawakkulgpt** (как и остальные ссылки на сайте).
Если заведёте отдельный **канал** для дропов (например `t.me/tawakkulbrand`) — скажите, поменяю ссылку
в секции дропа на канал за 1 минуту.
